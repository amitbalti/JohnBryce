import { useEffect, useState } from "react";
import "./Youtube.css";

function Youtube(): JSX.Element {
  const [refresh, setRefresh] = useState(false);

  useEffect(() => {
    // redux
  }, []);

  return <div className="Youtube">{}</div>;
}

export default Youtube;
